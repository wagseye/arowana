import Database from "database-connector";
import { Test } from "testing";

export default class MyTestClass {
  static async testMethod() {
    // const res = await Database.query('SELECT * FROM my_table');
    // Test.assertEquals(1, res.length);
  }
}
